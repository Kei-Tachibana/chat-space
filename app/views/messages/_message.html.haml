.message
    .upper-message
        .user__name
            = message.user.name
        .date
            = format_pasted_time(message.created_at)
    .lower-message
        - if message.body.present?
            = message.body
        = image_tag message.image.url, class: 'lower-message__image' if message.image.present?
